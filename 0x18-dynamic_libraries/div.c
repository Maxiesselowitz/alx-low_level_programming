#include <stdlib.h>

/**
 * my_div - the division of the two integers
 *
 * @a: the first integer
 *
 * @b: the second integer
 *
 * Return: the result
 */

int my_div(int a, int b)
{
        return (a / b);
}
