#include <stdlib.h>
#include "add.h"

/**
 * add - adds the two integers
 *
 * @a: the first integer
 *
 * @b: the second interger
 *
 * Return: the addtion of the two integers
 */

int add(int a, int b)
{
	return (a + b);
}


#include <stdlib.h>

/**
 * my_div - the division of the two integers
 *
 * @a: the first integer
 *
 * @b: the second integer
 *
 * Return: the result
 */

int my_div(int a, int b)
{
	return (a / b);
}


#include <stdlib.h>

/**
 * mod - the mod of the two integers
 *
 * @a: the first integer
 *
 * @b: the second integer
 *
 * Return: the result
 */

int mod(int a, int b)
{
	return (a % b);
}


#include <stdlib.h>
#include "add.h"

/**
 * mul - it multiplies two integers
 *
 * @a: the first integer
 *
 * @b: the second integer
 *
 * Return: the result
 */

int mul(int a, int b)
{
	return (a * b);
}


#include <stdlib.h>

/**
 * sub - it subtracts two integers
 *
 * @a: the first integer
 *
 * @b: the second integer
 *
 * Return: the result
 */

int sub(int a, int b)
{
	return (a - b);
}
